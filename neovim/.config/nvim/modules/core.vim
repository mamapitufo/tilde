" Should be handled by vim-sensible, but might be needed before it loads
set backspace=indent,eol,start

" Default indent, vim-sleuth should handle it for most cases
set tabstop=2
set shiftwidth=2
set expandtab

set number
set cursorline

set noswapfile

set splitbelow
set splitright

set completeopt-=preview

