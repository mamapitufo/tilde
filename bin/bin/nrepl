#!/usr/bin/env sh
exec clojure -Sdeps '{:deps {nrepl {:mvn/version "0.8.2"} cider/cider-nrepl {:mvn/version "0.25.4"}}}' \
    -m nrepl.cmdline \
    --middleware '["cider.nrepl/cider-middleware"]'
    --interactive
